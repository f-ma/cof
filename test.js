var React = require('react');
var PropTypes = React.PropTypes;

var ReactClassEx = React.createClass({

  render: function() {
    return (
      <div />
    );
  }

});

module.exports = ReactClassEx;
